/**
 * Custom event name for triggering a render.
 */
export const EVENT_RENDER = 'selective.render';

/**
 * Custom event name for triggering a render.
 */
export const EVENT_RENDER_COMPLETE = 'selective.render.complete';

/**
 * Custom event name for triggering a field unlock.
 */
export const EVENT_UNLOCK = 'selective.unlock';
